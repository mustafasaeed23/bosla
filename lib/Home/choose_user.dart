import 'package:flutter/material.dart';

class ChooseUser extends StatelessWidget {
static const String routeName = 'choose_user';
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
